server:
  port: 8899

uet:
  grade:
    host: http://112.137.129.30/viewgrade
    term.code: 1
    education.type: 0
    account:
      username: ${UET_GRADE_USERNAME}
      password: ${UET_GRADE_PASSWORD}
  course:
    api: https://112.137.129.87/congdaotao/module/dsthi_new/index.php?r=lopmonhoc/napmonthi

spring:
  jpa:
    show-sql: true
    hibernate.ddl-auto: none

    database-platform: org.hibernate.dialect.MySQLCustomDialect
  datasource:
    url: jdbc:mysql://localhost:3306/uetgrade?useSSL=false&useUnicode=true&characterEncoding=utf-8
    username: ${UET_GRADE_DB_USERNAME}
    password: ${UET_GRADE_DB_PASSWORD}
    driver-class-name: com.mysql.jdbc.Driver

messenger:
  server: http://localhost:8888

ws:
  messenger: ${messenger.server}/services/Messenger

logging:
  level:
    net.bqc.uss: DEBUG
